# WikiVisage - Active Learning Facial Recognition for Wikimedia Commons
# Python 3.11+

# face_recognition_models uses pkg_resources, which was removed in setuptools 82+
setuptools<81

# Web framework
Flask==3.1.3
gunicorn==23.0.0

# OAuth 2.0
requests-oauthlib==2.0.0
requests>=2.32.0

# Database
PyMySQL==1.1.1
cryptography>=43.0.0

# Face recognition
# face-recognition depends on "dlib" (source-only, needs CMake/C++ to compile).
# This fork is identical but depends on "dlib-bin" (pre-compiled wheels) instead,
# avoiding both build-time OOM on Toolforge and local compilation failures.
dlib-bin>=19.24.6
face-recognition @ git+https://github.com/DiFronzo/face_recognition@d632e2e8d5b5b286d944c333f3ff4433dafabb19

# Image processing
Pillow>=10.4.0
scipy>=1.14.0

# Server-side sessions
Flask-Session==0.8.0

# Rate limiting
Flask-Limiter==3.8.0

# Internationalization
Flask-Babel==4.0.0

# Environment variables from .env files
python-dotenv==1.1.0
